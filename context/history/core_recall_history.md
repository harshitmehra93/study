# Core-Recall Evidence Archive

Sparse cold storage for meaningful guidance, corrections, and progression.
`context/core_recall.md` owns current state. Load only the selected item's
section.

## 1. BFS traversal and unweighted shortest-path proof

Correct full implementation using mark-on-enqueue, FIFO traversal, distance assignment, parent tracking, and backward path reconstruction. Recall initially marked visited on dequeue, described the frontier as an MST level, and gave `O(V)` time; corrected to the invariant that the queue holds vertices in nondecreasing shortest distance and to `O(V + E)` time with `O(V)` extra space. The first-discovery shortest-path proof required guidance, so redo that explanation independently.

## 2. DFS recursive traversal

Independently recalled the DFS forest, WHITE/GREY/BLACK lifecycle, discovery and finish timestamps, traversal from every remaining WHITE vertex, and O(V + E) time with O(V) color/time storage and O(V) worst-case recursion stack. The pseudocode initially called the helper unconditionally and used independent color branches, but Harshit identified this as omitted pseudocode detail rather than a model gap: recurse only across a WHITE edge, mark GREY on entry, and BLACK after all outgoing edges are examined. Correctly traced `0:(1,6)`, `1:(2,5)`, `2:(3,4)`, and disconnected root `3:(7,8)`.

## 3. DFS iterative traversal

Stack simulation, neighbor ordering, visited timing, difference from recursive DFS.

Recall 2026-07-27 (L2 review): The first skeleton modeled binary-tree preorder rather than adjacency-list DFS. After feedback, Harshit moved to graph neighbors and recovered a mark-on-pop duplicate guard; a temporary switch to FIFO required correction back to a stack. He then identified reverse adjacency iteration to reproduce recursive preorder. `O(V + E)` time was correct, but the claimed `O(V)` stack bound did not account for duplicate pending entries in the mark-on-pop version, and the stack invariant remained imprecise. Harshit clarified that exact iterative discovery/finish simulation using `(vertex, nextNeighborIndex)` frames had never been learned; the coach introduced it during this session. Do not treat that subtopic as failed recall or require it in the next ordinary iterative-DFS redo; defer it as optional later learning.

## 4. Edge classification

Tree, back, forward, and cross edges using DFS colors and discovery times.

Recall 2026-07-27 (L1 review): Harshit independently recalled the white/gray/black DFS states; tree, back, forward, and cross classifications; the self-loop case; disconnected traversal; and `O(V + E)` time with `O(V)` traversal space. The requested timestamped trace and classification of every example edge were not demonstrated after a focused follow-up. Storing classifications for all edges would require `O(E)` output space. Redo one complete timestamped trace independently.

## 5. Topological sort

Correctly identified DFS topological sorting and cycle detection through an edge to a GREY vertex. Recall needed correction on traversal across a DFS forest: there is no special starting vertex, all roots share one color array and result list, and vertices must be ordered by decreasing—not increasing—finish time. Appending each vertex on finish and reversing once is equivalent. For every DAG edge `u -> v`, `finish(u) > finish(v)`, including when `v` was completed by an earlier DFS root. O(V + E) time; O(V) auxiliary space for colors, result, and worst-case recursion stack, or O(V + E) total when adjacency storage is included.

## 6. Undirected cycle detection

DFS with parent exclusion; visited neighbor that is not parent means cycle.

Recall 2026-07-27 (L1 pass): Harshit gave a correct DFS forest trace and `O(V + E)` time. His initial gray-neighbor condition incorrectly included the reverse view of a tree edge. After that ambiguity was identified, he independently repaired the model using one shared visited identity per undirected edge: an unvisited edge to a gray vertex is a cycle, while an already processed edge is skipped. He then correctly accounted for the edge flags, colors, and recursion as `O(V + E)` auxiliary space. The standard parent-parameter variant avoids the `O(E)` edge-state overhead.

## 7. Count simple paths in DAG

For a verified DAG and fixed destination, use base case `count(destination) = 1` and recurrence `count(u) = sum(count(v))` over outgoing neighbors, memoized by node. No path-set cycle guard is needed after the DAG precondition is established.

Recall 2026-07-27 (L1 review): Harshit first produced a correct path-local backtracking enumeration but omitted memoization, leaving exponential recomputation on converging DAG branches. After that issue and the shared-suffix state were identified, he added the correct memo lookup/store around `count(node) = sum(count(neighbor))`, retained `count(target) = 1`, and recognized that dead ends yield zero. He initially gave `O(VE)` time, then corrected it to `O(V + E)` after edge-scan accounting feedback; memoization and recursion use `O(V)` auxiliary space. Redo the memoized suffix recurrence and complexity independently.

## 8. Relaxation primitive

`dist[v] > dist[u] + w(u,v)` update, parent update, unreachable guard.

## 9. Dijkstra

Non-negative weights, min-priority frontier, settled shortest-distance invariant. Review exposed incorrect muscle memory: using a `PriorityQueue<Node>` whose comparator reads mutable external distances. Corrected toward Java lazy Dijkstra with immutable `(node, distance)` states, successful-relaxation reinsertion, and stale-entry skipping. Still rehearse textbook `decreaseKey` vs Java lazy variants until the distinction is automatic.

## 10. Bellman-Ford

Independently recalled distance initialization, repeated edge relaxation, and parent updates. Corrections were needed to skip edges from infinite-distance sources, distinguish `V - 1` complete edge passes from individual relaxations, and prove the bound: without a reachable negative cycle, a shortest path can be simple and therefore has at most `V - 1` edges; after pass k, shortest routes using at most k edges are covered. Early termination requires an entire pass with no successful relaxation and means reachable distances have converged, not that unchanged nodes are necessarily unreachable. An extra successful pass implies a reachable negative cycle. Complexity is O(VE) time and O(V) distance/parent space. On the example, the negative edge `1 → 2 (-2)` produces the stable candidate `4 - 2 = 2`; it is not repeatedly subtracted. Final distances are `[0,4,2,5]`, and a negative edge alone is not a negative cycle.

## 11. Kruskal

Independently identified Kruskal's algorithm: process edges by increasing weight and accept an edge only when its endpoints are in different DSU components. Correctly selected `(0,1)`, `(2,3)`, and `(1,2)` for total weight 4. Completion detection needed correction: a visited-vertex set can contain every vertex while the selected forest is still disconnected; success requires exactly `V - 1` accepted edges or one remaining DSU component. Cycle safety comes from joining disjoint components, while minimum-weight optimality follows from the cut/exchange argument. Heap processing costs O(E log E) because up to E removals each cost O(log E); DSU operations are amortized O(alpha(V)), giving O(E log E) total time and O(E + V) auxiliary storage with a heap and DSU.

## 12. Prim

This is a miss and need to be reread. Recall initially described Kruskal: globally heapify all edges and join DSU components. Guided correction established Prim's defining invariant: start from one vertex and grow one connected tree; the priority queue contains crossing candidates added from visited vertices, and a candidate is accepted only when its destination is unvisited. Correctly traced edges `(0,1,1)`, `(1,2,2)`, and `(2,3,3)` for total weight 6. Textbook decrease-key Prim stores each unvisited vertex once with `key[v]` equal to its cheapest known connection from the tree and updates that record when a lighter crossing edge appears. Java lazy Prim instead inserts immutable `(vertex,weight,parent)` candidates and skips a record when its vertex is already visited. At most O(E) records give O(E log E), commonly O(E log V), time and O(V + E) auxiliary storage. If the heap empties before V vertices are visited, the graph is disconnected; restarting from each component produces a minimum spanning forest.

## 13. Forest disjoint set

Correctly recalled the forest model, representative lookup, path compression, same-component no-op, and union-by-size direction. Initial trace attached node `2` beneath non-root argument `1`; corrected to the key invariant that union links representative roots only. With root-as-null convention, the final example has `1,2,3 → 0`, `4 → 3`, and `5` separate; `find(4)` compresses `4 → 0`. Complexity needed correction: `union` and `connected` include two finds. With union-by-size/rank plus path compression, operations are amortized O(α(n)) and storage is O(n).

Recall 2026-07-29 (L1 review): Recalled a forest representation with parent pointers, `parent == null` for roots, and path compression. Material guidance was needed to distinguish the linked-list union cost from forest DSU, state that union links roots only, and give the union-by-rank/size plus path-compression amortized `O(α(n))` bound. Redo independently.

## 14. Linked-list disjoint set

Representative pointer, union cost tradeoff, why forest DSU is preferred.

## 15. Min-heap operations

Correctly explained and traced insert, sift-up, extract-min, and sift-down after guidance. Heap-order knowledge was solid, but recall initially omitted the complete-tree shape invariant, did not state that sift-down must choose the smaller child, treated `heap[0] == null` as the emptiness source of truth instead of logical size, and gave `O(n log n)` for bottom-up construction. Rehearse: complete tree filled left-to-right; parent `(i - 1) / 2`, children `2i + 1` and `2i + 2`; `O(log n)` insert/extract, `O(1)` peek and iterative auxiliary space, `O(n)` bottom-up heapify, and `O(n)` backing storage.

## 16. Hash table model

Correctly recalled bucket indexing, separate chaining, collision-list search, full rehashing into a doubled array, and average O(1) versus worst-case O(n) operations. The substantive correction was resize accounting: load factor is `entryCount / bucketCount`, not the proportion of non-null buckets; a table with many entries in one bucket can therefore reach its threshold while most buckets remain null. Normalize a possibly negative hash into `[0, capacity)` (for example with `Math.floorMod`), and recompute bucket indexes during resize because the capacity used by the index function changes.

## 17. BST traversal/search/min/max

Recalled leftmost minimum, rightmost maximum, inorder `left → root → right`, and height-dependent search bounds. With distinct keys, the ordering invariant needed correction from `left <= root < right` to strict `left < root < right`. The iterative search branches were initially reversed: when `node.value < target`, move right; when `node.value > target`, move left. Correct `search(7)` trace in the example is `8 → 3 → 6 → 7`. Empty search/min/max return null under the chosen contract. Search, minimum, and maximum take O(h): O(log n) balanced and O(n) skewed. Inorder always takes O(n) time, with O(h) recursion space—O(log n) balanced and O(n) skewed rather than always logarithmic. Redo the search skeleton independently after spacing.

## 18. Successor / predecessor

Right-subtree min case; ancestor walk case.

Recall 2026-07-29 (L1 review): Independently recalled the subtree cases: successor is the leftmost node in the right subtree, and predecessor is the rightmost node in the left subtree. Material guidance was needed for the ancestor cases: without a right (left) subtree, walk upward until first arriving from a left (right) child; reaching the root means no successor (predecessor). The final iterative parent-pointer algorithm has `O(h)` time and `O(1)` auxiliary space, not `O(h)` stack space. Redo independently.

## 19. BST insertion/deletion

Correctly recalled leaf and one-child deletion and proposed a valid two-child alternative to successor-value copying: promote the deleted node's right subtree, find that subtree's minimum node, and attach the deleted node's old left subtree as the minimum node's left child. Precision corrections: the minimum/leftmost node need not be a leaf but has no left child; the strict BST invariant is `left values < node < right values`; all old-left values are below the deleted key and therefore below every old-right value, which proves the attachment is safe. Root deletion cannot merely skip the parent update—the external root reference must become null, the only child, or the promoted subtree. Runtime is O(h), giving O(log n) when balanced and O(n) when skewed; parent pointers make final rewiring O(1), not the search. Auxiliary space is O(1) iteratively or O(h) recursively.

## 20. Trie insert/search/prefix

Independently wrote correct recursive insertion and exact-word search using character paths and an `isWord` terminal marker. Prefix lookup initially changed the boolean API into descendant enumeration and then reused exact-word search, which incorrectly makes `startsWith("app")` false after inserting only `"apple"`. Guided correction separated path existence from terminal-word membership. Correct trace: after inserting `"apple"`, exact `"apple"` is true, exact `"app"` is false, and prefix `"app"` is true; after inserting `"app"`, exact `"app"` becomes true. Operations take O(L) time. Because this implementation is recursive, each operation uses O(L) call-stack space rather than O(1); iterative traversal would use O(1). Insertion may allocate up to L new nodes, each with a fixed alphabet array, so O(A × L) worst-case allocation or O(L) for constant alphabet size A.

## 21. Trie keys/delete

DFS collection, delete pruning only when node has no children and is not terminal.

Recall 2026-07-29 (L1 review): Independently recovered prefix-node lookup, DFS collection with a backtracked `StringBuilder`, and the core deletion return invariant: a child is prunable only when it has no children and is not terminal. Material guidance was needed for missing-child no-op handling, the child-presence scan, and the recursive bounds. `keysWithPrefix` takes `O(p + T)` time for prefix length `p` and total characters/nodes traversed across returned words `T`; its recursion uses depth-bounded stack space in addition to the output. Recursive deletion takes `O(L)` time and `O(L)` call-stack space for word length `L`. Redo independently.

## 22. Amortized resizing

Correctly distinguished logical size from backing-array capacity and recalled O(1) indexed get/set and O(n) storage. Recall initially mixed in the hash-table 75% load-factor threshold and then confused the new append with an existing-element copy. Correct trace from capacity one: appending `10,20,30,40,50` triggers resize-copy counts `0,1,2,0,4`, ending at capacity eight with seven existing elements copied. Across n appends, geometric resize copies `1 + 2 + 4 + ...` total O(n); adding n ordinary writes remains O(n), so append is amortized O(1) even though a resizing append is O(n).

## 23. Singly linked list operations

Head/tail handling, insertion/deletion edge cases, pointer mutation order.

Recall 2026-07-29 (L1 pass): Independently designed a singly linked list with `head`, `tail`, and `size`. Correctly handled front insertion, tail append, linear search, and delete for empty, one-node, head, tail, and absent-value cases; tail deletion reassigns `tail` to the predecessor. Endpoint insertions are `O(1)` time and `O(1)` auxiliary space; search and delete are `O(n)` time with `O(1)` auxiliary space.

## 24. Doubly linked deque operations

Chose dummy head/tail sentinels and independently wrote correct O(1) `addFirst` and `addLast` pointer rewiring. Initial `removeFirst` lacked empty handling, and the proposed `size == 0` guard required the additional invariant that size is initialized and updated on every mutation. After prompting, wrote a correct explicit `removeLast`: save `tail.prev`, move `tail.prev` to the removed node's predecessor, repair `newLast.next = tail`, decrement size, and return the removed node/value. With a successful-removal decrement added to `removeFirst`, the single-element case restores `head.next = tail` and `tail.prev = head`. Invariant: size equals the number of real nodes, reciprocal `next`/`prev` links are consistent, and `size == 0` iff both sentinels point directly to each other. All four endpoint operations are O(1); storage is O(n). Correctly traced `[10] → [20,10] → [20,10,30] → [10,30] → [10]`. Redo all four skeletons independently after spacing.

## 25. Combinations generation

Backtracking choice order, start index, include/exclude or loop recursion.

Recall 2026-07-29 (L1 review): Initially restarted choices at `1` and used membership checks, generating permutations of each combination. Guided correction established the increasing-choice invariant: loop from `start` through `n`, choose `i`, and recurse with `i + 1`; this removes duplicates without `contains`. `k = 0` returns one empty combination, while `k > n` returns no combinations. The literal unpruned recursion visits `sum(C(n,j), j=0..k)` states, up to `O(2^n)`, plus `O(k * C(n,k))` output-copy work; auxiliary recursion/path space is `O(k)`. Bounding the loop by the number of remaining choices avoids hopeless branches and gives the usual output-sensitive `O(k * C(n,k))` time. Redo independently.

## 27. Prefix-state frequency counting

Learning 2026-08-10 (Major help): Submitted a correct `O(n^2)` Subarray Sum Equals K implementation using a prefix array and asked how to optimize it. The one-pass frequency-map model and essential identity were supplied: at current prefix `S`, every earlier prefix equal to `S - k` owns one valid subarray ending at the current position. Seed frequency `0 -> 1` for subarrays starting at index zero, query before inserting the current prefix, retain duplicate frequencies rather than only membership, and use `long` for accumulated sums. In the immediate guided implementation, the overall one-pass structure and query-before-insert order were correct, but `containsKey` plus `count++` counted only one matching prefix and `put(prefix, 1)` erased repeated-prefix frequency. The resulting corrected algorithm is `O(n)` time and `O(n)` auxiliary space. This was guided installation, not independent evidence; redo the model, invariant, and implementation on a different representative after spacing.

## 28. Fenwick tree point updates and prefix queries

Learning/review 2026-08-11 (Help=None during the submitted artifact; timing not observed): Harshit independently submitted a working 1-based Fenwick tree with defensive input/output copies, low-bit parent/next navigation, inclusive prefix queries, and assignment updates implemented by propagating the value delta. The focused `FenwickTreeTest` suite passed all 51 tests, including empty and invalid-index handling, negative values, multiple sizes, and repeated updates. The implementation builds in `O(n log n)`, queries and updates in `O(log n)`, and uses `O(n)` space. Review follow-ups are to use `long` for aggregates when sums may exceed `int`, express the invariant that `tree[i]` owns `(i - lowbit(i), i]`, prefer the conventional positive-index loop over a `-1` sentinel, and add randomized differential tests. Coordinate compression and frequency/order-statistic transfer were not part of this artifact, so AT17 is only partially installed. Review feedback was post-assessment and does not change the recorded `Help=None` evidence.

Learning transfer 2026-08-11 (Major help; timing not observed): Submitted a correct LeetCode 307 `NumArray` solution by inspection. It wraps a 1-based Fenwick tree, computes inclusive `[left, right]` sums as two prefix sums with `prefixSum(-1) = 0`, performs assignment updates through a stored-array delta, uses `long` internally, and builds in `O(n)` by forwarding each accumulated node once to its next update ancestor. Construction is `O(n)`, each operation is `O(log n)`, and storage is `O(n)`; the final cast is safe under this problem's constraints. The linear-build algorithm and its correction from the query-parent direction to the update-next direction were supplied in the immediately preceding teaching, so this reinforcement is not independent even though the original point-update and prefix-query implementation had already been independently demonstrated. The pasted solution was reviewed but not executed from the repository.

Learning attempt 2026-08-11 (Nudge; timing not observed): For the generic inversion-count drill, Harshit first implemented the direct pair-enumeration baseline. It correctly applies the strict `nums[i] > nums[j]` definition, preserves the input, and passed all eight initial tests covering empty, singleton, sorted, reverse-sorted, duplicate, negative, and mixed inputs. The nested loops take `O(n^2)` time rather than the requested `O(n log n)`, and the internal `int` counter can overflow despite the `long` return type. This establishes the correctness baseline but not frequency-Fenwick or coordinate-compression transfer. The next repair is deliberately split: first replace the suffix scan with frequency prefix queries on a small fixed value domain, then add compression separately.

Guided repair attempt 2026-08-11 (Major help; timing not observed): After teaching the frequency-Fenwick model and coordinate compression, Harshit correctly cloned and sorted the input, assigned equal values the same 1-based rank, preserved original order in a ranked array, and implemented additive Fenwick frequency updates. The right-to-left loop inserted every rank but did not query and accumulate the number of smaller elements before each insertion; the method instead returned one final prefix frequency, so it counted inserted elements rather than inversion pairs. The prefix method also mixed a zero-based external offset with the otherwise 1-based rank/update API. The focused suite passed 3 of 8 tests; the four-element headline example passed coincidentally because its length equalled its expected inversion count. Repair by using one consistent 1-based Fenwick contract and, for every element from right to left, querying the strict-smaller prefix before inserting the current rank.

Guided repair continuation 2026-08-11 (Major help): The coach supplied the exact `long` accumulator and right-to-left query-before-insert loop, and Harshit pasted that snippet into the implementation. The resulting code improved the suite to 7 of 8 passing, but this does not demonstrate that Harshit derived or independently understood that loop. The remaining duplicate failure (`4` expected, `6` returned) is localized to the previously identified indexing mismatch: the caller supplies the 1-based boundary `currentRank - 1`, but `sum` adds one again and therefore includes the current rank and equal values. Remove that extra conversion so the prefix method and insertion both use the same 1-based Fenwick contract.

Clarification 2026-08-11: Harshit explicitly stated that he could not independently formulate the frequency counting. Treat the frequency-Fenwick inversion model, query-before-insert invariant, and compression transfer as taught but not yet installed. Retain only the earlier direct `O(n^2)` inversion baseline and original point-update/prefix-query Fenwick implementation as independently demonstrated evidence. Step back to a plain fixed-domain frequency-array drill before using Fenwick or coordinate compression again.

Guided transfer 2026-08-12 (Major help; timing not observed): Harshit submitted a LeetCode 315 solution after the prior direct teaching of frequency Fenwick, right-to-left query-before-insert, strict `rank - 1`, coordinate compression, and duplicate handling. By inspection, the solution correctly clones and sorts values, assigns duplicates one rank, scans from right to left, queries smaller frequencies before insertion, and returns one count per original position. This is guided application, not independent evidence. A material implementation issue remains: `ArrayList.add(0, count)` shifts the accumulated suffix on every iteration and makes result assembly `O(n^2)`. Store each count directly at its original index or use an actual constant-time prepend, yielding `O(n log n)` total time and `O(n)` space. The submitted code was not saved in the repository or executed locally.

Guided transfer completion 2026-08-12 (Major help): Harshit replaced front insertion with direct writes into an `Integer[]` and returned `Arrays.asList(result)`. By inspection, the completed LeetCode 315 solution now has `O(n log n)` time and `O(n)` space while retaining correct compression, duplicate exclusion, right-to-left query-before-insert, and original-position output. The code was pasted in chat rather than saved or executed locally. This completes the guided transfer only; independently recover the model after spacing.

## 30. Manacher transformed-string radius preprocessing

Guided learning/review 2026-08-17 (Major help; timing not observed): After direct teaching of Manacher symmetry, mirror clipping, separator transformation, and interval mapping, Harshit submitted a working single-radius implementation. It transforms the input to `#a#b#...#`, maintains exclusive `(L,R)` boundaries for the rightmost-reaching transformed palindrome, clips the mirrored starting radius by `R - i`, expands from the first unverified layer, and answers original half-open interval queries for both parities. All 10 focused tests passed, and a fixed-seed differential check covering every non-empty interval of 2,000 random strings of length 1–30 also passed. The implementation's radius starts at zero and first compares the center with itself, so the stored value is ultimately center-inclusive; the query arithmetic is correct but can be expressed more directly as `center = left + right` and `requiredRadius = right - left + 1`. Follow-ups are to validate or explicitly define invalid/empty intervals, encapsulate immutable fields, apply the preprocessing to CT005 3327, and later recover the invariant, mapping, and linear-time argument independently. This is guided installation, not independent evidence.

## 31. Euclidean GCD and overflow-safe LCM

Learning/review 2026-08-18 (Help=None for the submitted artifact; timing not observed): Harshit started a Number Theory core skill with array GCD and LCM implementations based on generating primes through the maximum input and repeatedly dividing all or some values. The submitted positive examples all passed: one JUnit method for GCD and one for LCM. The factor-exponent model is correct for those small positive inputs, but the artifact is not yet constraint-safe. LCM loops forever for any input containing zero because zero remains divisible by every processed prime; GCD loops forever for an empty array and for all-zero arrays; all-negative GCD inputs can miss factors because `max` stays zero; both methods mutate the caller's array; prime generation through `max` is infeasible for large values; and `int` LCM multiplication silently overflows. The repair target is a non-mutating fold using the Euclidean invariant `gcd(a,b) = gcd(b,a mod b)`, normalized non-negative inputs, an explicit empty/zero contract, and `lcm(a,b) = abs((a / gcd(a,b)) * b)` using `long` or checked overflow. Add focused zero, negative, empty-contract, non-mutation, overflow, large-value, and randomized differential tests. Review feedback is post-assessment and does not change the recorded `Help=None` evidence.

Guided repair 2026-08-18 (Major help; timing not observed): Harshit cloned the input at the start of both array methods, correctly removing the caller-mutation side effect, and the unchanged two focused test methods still pass. No tests were added and the prime-factor control flow remains otherwise unchanged. Consequently, LCM still does not terminate for any array containing zero; GCD still does not terminate for empty or all-zero arrays; all-negative handling, value-scaled prime generation, and `int` LCM overflow remain material. Continue with the previously supplied Euclidean pair primitive and fold rather than extending the factorization loop. This repair is incomplete and guided.

Guided repair continuation 2026-08-18 (Major help; timing not observed): Harshit added early zero guards and corresponding examples. The LCM guard is correct and eliminates the prior non-termination because `lcm(0,n) = 0`. The GCD guard encodes an incorrect identity: it returns zero whenever any operand is zero, and the new test expects `gcd(1,0,16) = 0`, whereas the correct result is `1` because `gcd(0,n) = |n|`; only `gcd(0,0)` is zero under the chosen convention. Both test methods pass because the GCD assertion repeats the implementation defect. Empty GCD still does not terminate, and sign normalization, value-scaled prime generation, and `int` LCM overflow remain unresolved. Stop extending the prime-factor loop and implement/test the Euclidean pair primitive before rebuilding the array folds. This continuation is incomplete and guided.

Guided repair continuation 2026-08-18 (Major help; timing not observed): Harshit replaced the active array GCD path with a fold over a recursive Euclidean pair method, while retaining the old factorization implementation under `gcdB`. This corrects the algorithm choice for ordinary positive nonzero inputs, and the existing suite passes. The pair base case is still inverted: `b == 0` returns `b` rather than `|a|`, so `gcd(0,16)` and the folded `gcd(1,0,16)` return zero; the unchanged test still expects the wrong zero result. Negative normalization and an explicit empty-array contract are also absent, and the legacy factorization code obscures which implementation is authoritative. Correct the pair primitive and its oracle, add focused pair/array edge cases, then remove the obsolete path before proceeding to LCM. This continuation is incomplete and guided.

Guided repair continuation 2026-08-18 (Major help; timing not observed): Harshit corrected the Euclidean pair base case to return `a` when `b == 0`, so positive inputs and mixed-zero cases such as `{1,0,16}` now compute correctly. The array wrapper then adds `if (a == 0) return 1`, and the updated suite expects `gcd(0,0) = 1`; both contradict the chosen convention `gcd(0,0) = 0` and make every all-zero array incorrect. The two focused test methods pass because the all-zero oracle repeats that wrapper defect. Remove the final zero-to-one conversion and expect zero for all-zero input. Negative normalization, empty-array behavior, legacy-factorization cleanup, and Euclid-based overflow-safe LCM remain subsequent work. This continuation is incomplete and guided.

Guided repair continuation 2026-08-18 (Major help; timing not observed): Harshit removed the all-zero conversion, corrected the test to expect `gcd(0,0) = 0`, and normalized ordinary negative operands before a recursive Euclidean fold. The current GCD is correct for non-empty inputs excluding the unhandled `Integer.MIN_VALUE` representability case; empty input still indexes `nums[0]`, the `a == 0 && b == 0` check is unreachable after the `b == 0` return, and stale prime imports/state remain. LCM correctly returns zero when any operand is zero and preserves its input, but it still uses value-scaled prime generation, returns incorrect results for negative-only values with factors above three (for example `lcm(-5,-10)`), and silently overflows `int`. Both existing test methods pass. Finish the GCD contract/cleanup, then implement LCM as a fold of normalized pairwise `abs((a / gcd(a,b)) * b)` using `long` and checked multiplication. This continuation is guided and not yet complete.

Guided repair continuation 2026-08-18 (Major help; timing not observed): Harshit defined empty-array GCD as zero and added a passing test, while retaining correct mixed-zero, all-zero, positive, and ordinary-negative Euclidean results. GCD is now correct for the tested domain; `Integer.MIN_VALUE` cannot be normalized into a non-negative `int`, and stale prime imports/state plus an unreachable zero check remain cleanup. LCM now returns `long`, normalizes ordinary negatives, preserves zero behavior, and passes new tests for `lcm(-5,-10) = 10` and `lcm(50000,49999) = 2,499,950,000`. It still generates primes through the maximum value instead of folding pairwise GCD, so runtime scales with the numeric value and the `Integer.MAX_VALUE` loop is not viable; repeated `long` multiplication is also unchecked and may wrap. Both focused test methods pass. The next repair is algorithmic: replace prime generation with pairwise divide-before-multiply LCM and use `Math.multiplyExact` or a documented overflow contract. This continuation remains guided and incomplete.
